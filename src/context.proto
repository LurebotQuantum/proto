syntax = "proto3";

package quantum;

message Context {
  string aid = 1; // adpater/im specific id for user
  string cid = 2; // adapter/im specific id for chat
  enum ChatType {
    PRIVATE = 0;
    GROUP = 1;
  }
  ChatType type = 3;
  string ext = 4;
}
